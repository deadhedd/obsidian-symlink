# Review, main, 2026-09-22

**Reviewed by**: GPT-5 (author model not provided)
**Scope**: 21 files, uncommitted
**Verdict**: Approve

## Summary
This change establishes a repository-owned `npm run release:check` workflow that runs the canonical verification gate, validates release metadata, constructs the two-file install artifact, and reports SHA-256 evidence. The implementation is small and fail-closed around release-directory and production-output types. No findings remain in the current uncommitted diff.

## Strengths
- `scripts/release-check.mjs` validates the existing artifact boundary before the canonical check and validates the resulting artifact after copying, while allowing only `main.js` and `manifest.json` as regular files.
- Focused tests cover successful construction, metadata mismatches, unexpected retained paths, canonical-check failure preservation, and each previously untested release-path and production-output type branch.
- The README, ignored output directory, release checklist, and specification consistently describe the same two-file release process.

## Test coverage
The configured Node test suite includes focused coverage for the new release command's success and failure paths, including the non-directory `release` path, non-regular allowed artifact entry, missing `main.js`, and non-regular `main.js` cases. The supplied local signal reports 41 passing tests; this review additionally ran `npm run typecheck` and `git diff --check`, both of which passed. The full test suite and release command were not rerun because this review was limited to read-only checks and writing this findings file.
