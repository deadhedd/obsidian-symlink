{
  "name": "symlink-notes",
  "version": "0.1.0",
  "private": true,
  "description": "Portable Markdown-native shortcuts for Obsidian notes.",
  "scripts": {
    "dev": "node esbuild.config.mjs --watch",
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && node esbuild.config.mjs",
    "test": "node tests/run.mjs"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "esbuild": "^0.25.0",
    "yaml": "^2.8.1",
    "obsidian": "^1.8.7",
    "typescript": "^5.8.0"
  }
}
